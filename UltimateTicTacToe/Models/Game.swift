//
//  Game.swift
//  UltimateTicTacToe
//
//  Created by Mason Raffo on 4/15/21.
//

import Foundation

struct Game {
    /**
     Finds the piece that is currently at the given position.
     
     - Parameter position: The index of the location to check.
     
     - Returns: 1 if the space is taken by X, 2 if the space is taken by O, 0 if the space is unclaimed.
     */
    func atPosition(position: Int) -> Int {
        return 0
    }
}
